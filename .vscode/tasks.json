{
    "tasks": [
        {
            "type": "cargo",
            "command": "make",
            "problemMatcher": [
                "$rustc"
            ],
            "label": "rust: cargo make build-flash-debug",
            "options": {
                "cwd": "${workspaceFolder}/h7",
            }
        }
    ]
}